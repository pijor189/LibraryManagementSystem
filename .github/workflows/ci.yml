name: CI Pipeline

permissions:
  contents: write

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.13']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-html

      - name: Lint with Ruff
        run: |
          ruff check . --output-format=github

      - name: Format check with Ruff
        run: |
          ruff format . --check

      - name: Run tests with reports
        run: |
          pytest tests/ -vv \
            --log-cli-level=INFO \
            --capture=tee-sys \
            --html=pytest_report.html \
            --self-contained-html \
            --cov=library \
            --cov-report=html

      - name: Prepare dashboard
        run: |
          mkdir public
          cp -r htmlcov public/coverage
          cp pytest_report.html public/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: public/

#
#  deploy:
#    permissions:
#      contents: write
#    needs: test
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Download artifact
#        uses: actions/download-artifact@v4
#        with:
#          name: reports
#          path: public
#
#      - name: Deploy to GitHub Pages
#        uses: peaceiris/actions-gh-pages@v4
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          publish_dir: public
#          force_orphan: true

#    - name: Lint with Ruff
#      run: |
#        ruff check . --output-format=github
#
#    - name: Format check with Ruff
#      run: |
#        ruff format . --check

#    - name: Run tests with pytest and coverage
#      run: |
#        pytest tests/ -vv --cov=library --cov-report=term-missing --cov-report=xml --cov-report=html --cov-report=term
#        --html=pytest_report.html --self-contained-html --log-cli-level=INFO --junitxml=report.xml --cov-fail-under=80
#
#    - name: Upload coverage to Codecov
#      uses: codecov/codecov-action@v4
#      with:
#        file: ./coverage.xml
#        flags: unittests
#        name: codecov-umbrella
#        fail_ci_if_error: false
#
#    - name: Archive coverage reports
#      if: always()
#      uses: actions/upload-artifact@v4
#      with:
#        name: coverage-report
#        path: htmlcov/
